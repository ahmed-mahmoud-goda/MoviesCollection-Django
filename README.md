A Django web app built with Pipenv for managing movies (CRUD operations).

Features:
- Add, edit, delete, and view movies
- Class Based Views
- Uses SQLite database
- Simple and responsive HTML templates
